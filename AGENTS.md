# moes-tavern Agent Instructions

- Follow the CAR tickets in `moes-tavern/.codex-autorunner/tickets/` in numeric order.
- No manual test steps. Prefer deterministic, headless tests.
- Keep contracts explicit and heavily tested around lifecycle/escrow/timeouts.

